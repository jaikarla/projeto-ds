services:

  backend:
    build:
      context: ./backend
    container_name: bpa-backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend
    container_name: bpa-frontend
    ports:
      - "80:80"
    depends_on:
      - backend